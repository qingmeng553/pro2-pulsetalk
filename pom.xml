<?xml version="1.0" encoding="UTF-8"?>
<!--
  pro2 点评贴吧社区 —— 单模块 SpringBoot 后端工程
  技术栈: JDK17 + SpringBoot3.2 + Sa-Token(Redis会话) + MyBatis-Plus + MySQL8 + Redis7 + MinIO
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 继承 SpringBoot 官方父工程，统一管理依赖版本 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.community</groupId>
    <artifactId>pro2</artifactId>
    <version>1.0.1</version>
    <name>pro2-community</name>
    <description>Redis 综合实战项目：点评贴吧社区（单模块后端）</description>

    <properties>
        <java.version>17</java.version>
        <mybatis-plus.version>3.5.7</mybatis-plus.version>
        <sa-token.version>1.39.0</sa-token.version>
        <minio.version>8.5.17</minio.version>
    </properties>

    <dependencies>
        <!-- ===================== Web / 校验 ===================== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- 参数校验(Jakarta Validation)，用于 DTO @NotBlank/@Size 等 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ===================== Redis(Spring Data Redis + Lettuce) ===================== -->
        <!-- Sa-Token 的 Redis 会话存储需要 Spring Data Redis 提供 RedisConnectionFactory -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <!-- Lettuce 连接池 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-pool2</artifactId>
        </dependency>

        <!-- ===================== Sa-Token 鉴权(SpringBoot3 版 + Redis Jackson 存储) ===================== -->
        <!-- Sa-Token 核心(含注解鉴权 SaCheckLogin / SaCheckRole) -->
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-spring-boot3-starter</artifactId>
            <version>${sa-token.version}</version>
        </dependency>
        <!-- Sa-Token 会话持久化到 Redis(Jackson 序列化，基于 Spring Data Redis)：
             引入后其 SaTokenDaoRedisJackson 会自动成为 SaTokenDao 实现，登录会话 token 全部落 Redis -->
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-redis-jackson</artifactId>
            <version>${sa-token.version}</version>
        </dependency>

        <!-- ===================== MyBatis-Plus(SpringBoot3 版) ===================== -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        <!-- MySQL 驱动(版本由 SpringBoot BOM 管理) -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ===================== MinIO 对象存储 SDK ===================== -->
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
            <version>${minio.version}</version>
        </dependency>

        <!-- Lombok 简化样板代码(仅编译期，不打包进 jar) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 测试依赖 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>pro2-${project.version}</finalName>
        <plugins>
            <!-- SpringBoot 打包插件：生成可执行 fat jar(target/pro2-1.0.1.jar)供 Dockerfile 复制 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
